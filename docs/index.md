# go-update

`go-update` provides self-update helpers for Go applications.

The module supports GitHub release discovery, generic HTTP update manifests,
and in-place binary replacement through the update service.

